#include "panel_2d.h"


bool Panel2D::init()
{
	//CC::CCDataCollection* cc_data = new CC::CCDataCollection();
	//cc_data->loadFile("cocos/DemoPlayer.ExportJson");  
	//p_armature = new CC::CCArmature();
	//p_armature->init(cc_data->getArmatureData());
	//p_armature->initAnimation(cc_data->getAnimationData());
	//p_armature->initTexture();
	return true;
}

void Panel2D::update( time_t time_elapsed )
{
	//p_armature->updateAnimation(time_elapsed / 1000.f);
}

void Panel2D::render( time_t time_elapsed )
{
	//p_armature->updateTransform(time_elapsed / 1000.f);
	//p_armature->render();
}